# 🚀 AI-SUM 更新与演进计划 (Roadmap & Updates)

> 本文档用于记录 AI-SUM 项目的历史更新以及未来版本（V6.0+）的计划安排。

---

## 📅 历史已完成更新

### V5.0 — 2026-05-18 (多时间尺度吸筹与量能指标追踪)
*   **多时间尺度留存计算**：在 `history_report.py` 中引入 `24h (1d)` 和 `72h (3d)` holder 留存率及 Top10 浓度漂移计算，精细捕捉主力短期资金变动。
*   **量价背离（vpd）4分桶扩展**：扩展为 4 种类型：
    *   `潜伏吸筹`（价平量增，主力暗中吃单，高正收益）
    *   `突破放量`（价涨量增，强势拉升，高正收益）
    *   `价涨量缩`（拉盘无力）
    *   `价跌量增`（恐慌抛售）
*   **庄家浮盈率 (pnl_ratio)**：从 `cost_basis_snapshots` 提取最新 `vwap` 成本与现价进行偏差对比，量化庄家浮盈度。
*   **出货背离预警 (whale_divergence)**：判定诱多出货（价格上涨 + 大户撤离），在长期报告中以 🚨 标记。
*   **表结构热追加**：`token_history` 数据库自动平滑追加 7 列并同步扩展 `INSERT` 落库逻辑（共 32 列）。
*   **报告面板重构**：长期报告「Holder 迁移矩阵」及「Top ACC 看板」同步升级表头列展示。

---

## 🎯 V6.0+ 积分权重联动计划 (Scoring Linkage Plan)

> **设计宗旨**：在 V5.0 基础特征平稳运行并积累足够回测样本的前提下，将新特征正式接入核心仲裁引擎（`arbitrator.py` / `meta-verdict/run.py`），使短期异常动量直接反馈并影响代币的 `meta_score` 评级。

### 1. 出货背离高危熔断 (Whale Divergence Penalty)
*   **逻辑**：当代币被检测到 `whale_divergence = 1` 时（价格拉升，但 Top10 庄家撤退幅度 >1%），视为高危出货逃顶信号。
*   **积分联动**：
    *   在 `arbitrator.py` 的仲裁逻辑中**强制扣减 -3.0 分**。
    *   直接将其生命周期状态由 `ACCUMULATING` 熔断转换为 `💀 DISTRIBUTING`。

### 2. 潜伏吸筹量能引导 (Stealth Accumulation Boost)
*   **逻辑**：当流动性判定出现 `潜伏吸筹`（价平量增，主力在暗中吃单），属于极高正收益的早期爆发前兆。
*   **积分联动**：
    *   在仲裁层**额外增加 +1.5 分**。
    *   帮助处于吸筹早期的黑马代币跨越中长线过滤门槛，尽早进入 `🎯 ACC` 关注看板。

### 3. 格局筹码溢价奖励 (Strong Hold Premium)
*   **逻辑**：当庄家处于高浮盈（`pnl_ratio > 100%`），但 24h 留存率极高（`retention_24h > 95%`），表明庄家高度看好后市，拒绝高位套现。
*   **积分联动**：
    *   在仲裁层**额外奖励 +1.0 分**（格局筹码溢价）。

---

## 🛠 开发执行路径

1.  **观察期 (2026-05-18 至 2026-05-25)**：通过 `tracking_validator.py` 和 `history_report.py` 观察 V5.0 写入 `token_history` 数据库的这 7 个指标与未来 3-7 天价格走势的匹配胜率，确认上述联动数值的合理性。
2.  **代码重构**：
    *   修改 `meta-verdict/arbitrator.py`，从 `token_history` 或直接在内存计算中调取 `whale_divergence`、`vpd` 和 `pnl_ratio`。
    *   在主仲裁类中增加对应的增减分逻辑分支。
3.  **灰度测试**：独立跑一轮 `run.py` 观察 watchlist 中 ACC 代币的变化情况，防止过度洗牌。
