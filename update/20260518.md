# 🎯 AI-SUM V6.0 积分权重联动设计计划

> **生成日期**：2026-05-18
> **状态**：🔴 **未执行** (Pending / 待一周特征稳定性观测)
> **目标**：在 V5.0 新特征落库 `token_history` 稳定运行后，将短期动能与背离信号正式接入核心仲裁层（`arbitrator.py`），联动影响代币的 `meta_score`。

---

## 🛠 联动判定设计规划 (未执行)

### 1. 诱多出货高危熔断 (Whale Divergence Penalty)
*   **状态**：🔴 **未执行**
*   **逻辑**：当代币被检测到 `whale_divergence = 1` 时（价格拉升，但 Top10 庄家撤退幅度 >1%），视为高危出货逃顶信号。
*   **积分联动**：
    *   在 `arbitrator.py` 的仲裁中**强制扣减 -3.0 分**。
    *   直接将其生命周期状态由 `ACCUMULATING` 熔断转换为 `💀 DISTRIBUTING`。

### 2. 潜伏吸筹量能引导 (Stealth Accumulation Boost)
*   **状态**：🔴 **未执行**
*   **逻辑**：当流动性判定出现 `潜伏吸筹`（价平量增，主力在暗中吃单），属于极高正收益的早期爆发前兆。
*   **积分联动**：
    *   在仲裁层**额外增加 +1.5 分**。
    *   帮助处于吸筹早期的黑马代币跨越中长线过滤门槛，尽早进入 `🎯 ACC` 关注看板。

### 3. 格局筹码溢价奖励 (Strong Hold Premium)
*   **状态**：🔴 **未执行**
*   **逻辑**：当庄家处于高浮盈（`pnl_ratio > 100%`），但 24h 留存率极高（`retention_24h > 95%`），表明庄家高度看好后市，拒绝高位套现。
*   **积分联动**：
    *   在仲裁层**额外奖励 +1.0 分**（格局筹码溢价）。

---

## 📅 下步开发执行路径

1.  **观察验证期 (2026-05-18 至 2026-05-25)**：通过 `tracking_validator.py` 观察 V5.0 数据特征在 `token_history` 的匹配胜率。
2.  **引擎重构**：修改 `meta-verdict/arbitrator.py` 以将新字段并入计算流。
3.  **灰度测试**：跑回测检验评分分布是否均匀，无误后正式上线。
